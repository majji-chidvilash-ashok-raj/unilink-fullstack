import { useState } from 'react';import { Link } from 'react-router-dom';import { motion } from 'framer-motion';import { Calendar, MapPin, Users, Check } from 'lucide-react';export default function EventCard({ event }) {  const [joined, setJoined] = useState(event.joined);  return (    <Link to={`/events/${event.id}`} className="block rounded-2xl bg-surface border border-border overflow-hidden hover:border-primary/20 transition-all group">      <div className="relative h-36 overflow-hidden">        <img src={event.image} alt="" className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />        <div className="absolute top-3 left-3">          <span className="px-2.5 py-1 rounded-full text-xs font-semibold bg-primary/20 text-primary-light backdrop-blur-sm">{event.category}</span>        </div>      </div>      <div className="p-4">        <h3 className="font-bold text-sm group-hover:text-primary-light transition-colors">{event.title}</h3>        <div className="mt-2 space-y-1">          <p className="text-xs text-text-muted flex items-center gap-2"><Calendar size={12} className="text-primary-light" /> {event.date}</p>          <p className="text-xs text-text-muted flex items-center gap-2"><MapPin size={12} className="text-primary-light" /> {event.location}</p>          <p className="text-xs text-text-muted flex items-center gap-2"><Users size={12} className="text-primary-light" /> {event.attendees} attending</p>        </div>        <motion.button whileTap={{ scale: 0.95 }} onClick={(e) => { e.preventDefault(); setJoined(!joined); }}          className={`w-full mt-3 py-2 rounded-xl text-xs font-semibold flex items-center justify-center gap-2 transition-all ${            joined ? 'bg-success/15 text-success border border-success/30' : 'gradient-primary text-white shadow-lg hover:opacity-90'          }`}>          {joined ? <><Check size={14} /> Joined</> : 'Join Event'}        </motion.button>      </div>    </Link>  );}