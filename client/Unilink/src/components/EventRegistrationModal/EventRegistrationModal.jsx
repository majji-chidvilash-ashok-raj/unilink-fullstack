import { useState } from 'react';import { motion, AnimatePresence } from 'framer-motion';import { X, User, Mail, Phone, FileText, Calendar, MapPin, Users, Loader2, CheckCircle } from 'lucide-react';import { useAuth } from '../../context/AuthContext';import { useToast } from '../Toast/Toast';import { eventService } from '../../services';export default function EventRegistrationModal({ event, isOpen, onClose, onRegistered }) {  const { user } = useAuth();  const { addToast } = useToast();  const [step, setStep] = useState(1);   const [loading, setLoading] = useState(false);  const [form, setForm] = useState({    name: user.name,    email: user.email,    phone: '',    reason: '',    dietary: '',    teamSize: '1',    experience: 'beginner',  });  const handleChange = (e) => setForm({ ...form, [e.target.name]: e.target.value });  const handleSubmit = async (e) => {    e.preventDefault();    setLoading(true);    try {      await eventService.joinEvent(event.id);      setStep(2);      onRegistered?.();      addToast(`Successfully registered for ${event.title}!`, 'event');    } catch (err) {      console.error("Failed to register for event:", err);      addToast(err.response?.data?.msg || 'Failed to register', 'error');    } finally {      setLoading(false);    }  };  const handleClose = () => {    onClose();    setTimeout(() => setStep(1), 300);  };  const inputClass = "w-full glass-input rounded-xl px-4 py-3 text-sm outline-none focus:border-primary/50 transition-colors";  if (!isOpen) return null;  return (    <AnimatePresence>      {isOpen && (        <div className="fixed inset-0 z-[100] flex items-center justify-center p-4">          {}          <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}            onClick={handleClose} className="absolute inset-0 bg-black/60 backdrop-blur-sm" />          {}          <motion.div            initial={{ opacity: 0, scale: 0.95, y: 20 }}            animate={{ opacity: 1, scale: 1, y: 0 }}            exit={{ opacity: 0, scale: 0.95, y: 20 }}            transition={{ type: 'spring', damping: 25, stiffness: 350 }}            className="relative w-full max-w-lg max-h-[90vh] overflow-y-auto rounded-2xl bg-surface border border-border shadow-2xl"          >            {}            <div className="sticky top-0 bg-surface/80 backdrop-blur-xl border-b border-border px-6 py-4 flex items-center justify-between z-10">              <h2 className="font-bold text-lg font-display">                {step === 1 ? 'Event Registration' : 'Registration Complete!'}              </h2>              <button onClick={handleClose} className="p-2 rounded-lg hover:bg-surface-light transition-colors text-text-muted">                <X size={18} />              </button>            </div>            {step === 1 ? (              <>                {}                <div className="px-6 py-4 bg-primary/5 border-b border-border">                  <h3 className="font-semibold text-sm">{event.title}</h3>                  <div className="flex flex-wrap gap-4 mt-2 text-xs text-text-muted">                    <span className="flex items-center gap-1"><Calendar size={12} className="text-primary-light" /> {event.date}</span>                    <span className="flex items-center gap-1"><MapPin size={12} className="text-primary-light" /> {event.location}</span>                    <span className="flex items-center gap-1"><Users size={12} className="text-primary-light" /> {event.attendees} attending</span>                  </div>                </div>                {}                <form onSubmit={handleSubmit} className="p-6 space-y-4">                  <div className="grid sm:grid-cols-2 gap-4">                    <div>                      <label className="block text-sm font-medium mb-1.5">Full Name *</label>                      <input type="text" name="name" value={form.name} onChange={handleChange} className={inputClass} required />                    </div>                    <div>                      <label className="block text-sm font-medium mb-1.5">Email *</label>                      <input type="email" name="email" value={form.email} onChange={handleChange} className={inputClass} required />                    </div>                  </div>                  <div className="grid sm:grid-cols-2 gap-4">                    <div>                      <label className="block text-sm font-medium mb-1.5">Phone Number</label>                      <input type="tel" name="phone" value={form.phone} onChange={handleChange} placeholder="+91 9876543210" className={inputClass} />                    </div>                    <div>                      <label className="block text-sm font-medium mb-1.5">Experience Level</label>                      <select name="experience" value={form.experience} onChange={handleChange}                        className={`${inputClass} appearance-none cursor-pointer`}>                        <option value="beginner">Beginner</option>                        <option value="intermediate">Intermediate</option>                        <option value="advanced">Advanced</option>                      </select>                    </div>                  </div>                  {(event.category === 'Tech' || event.category === 'Entrepreneurship') && (                    <div>                      <label className="block text-sm font-medium mb-1.5">Team Size</label>                      <select name="teamSize" value={form.teamSize} onChange={handleChange}                        className={`${inputClass} appearance-none cursor-pointer`}>                        <option value="1">Solo</option>                        <option value="2">2 Members</option>                        <option value="3">3 Members</option>                        <option value="4">4 Members</option>                        <option value="5">5+ Members</option>                      </select>                    </div>                  )}                  <div>                    <label className="block text-sm font-medium mb-1.5">Why do you want to attend? *</label>                    <textarea name="reason" value={form.reason} onChange={handleChange} rows={3} required                      placeholder="Tell us what excites you about this event..."                      className={`${inputClass} resize-none`} />                  </div>                  <div>                    <label className="block text-sm font-medium mb-1.5">Dietary Requirements</label>                    <input type="text" name="dietary" value={form.dietary} onChange={handleChange} placeholder="e.g. Vegetarian, Vegan, None"                      className={inputClass} />                  </div>                  <div className="flex gap-3 pt-2">                    <button type="button" onClick={handleClose}                      className="flex-1 py-3 rounded-xl bg-surface-light border border-border text-sm font-semibold hover:bg-surface-lighter transition-colors">                      Cancel                    </button>                    <motion.button type="submit" disabled={loading} whileTap={{ scale: 0.98 }}                      className="flex-1 py-3 rounded-xl gradient-primary text-white font-semibold shadow-lg hover:opacity-90 transition-all flex items-center justify-center gap-2 disabled:opacity-60">                      {loading ? <><Loader2 size={18} className="animate-spin" /> Registering...</> : 'Register Now'}                    </motion.button>                  </div>                </form>              </>            ) : (              <div className="p-8 text-center">                <motion.div initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ type: 'spring', damping: 15 }}                  className="w-20 h-20 rounded-full bg-success/15 flex items-center justify-center mx-auto mb-5">                  <CheckCircle size={40} className="text-success" />                </motion.div>                <h3 className="text-xl font-bold font-display mb-2">You're In! 🎉</h3>                <p className="text-sm text-text-muted mb-1">You've successfully registered for</p>                <p className="font-semibold text-primary-light mb-4">{event.title}</p>                <div className="rounded-xl bg-surface-light p-4 text-left text-sm space-y-2 mb-6">                  <p className="flex items-center gap-2"><Calendar size={14} className="text-primary-light" /> {event.date}</p>                  <p className="flex items-center gap-2"><MapPin size={14} className="text-primary-light" /> {event.location}</p>                  <p className="flex items-center gap-2"><User size={14} className="text-primary-light" /> {form.name}</p>                  <p className="flex items-center gap-2"><Mail size={14} className="text-primary-light" /> {form.email}</p>                </div>                <p className="text-xs text-text-muted mb-6">A confirmation email has been sent to {form.email}</p>                <motion.button whileTap={{ scale: 0.95 }} onClick={handleClose}                  className="px-8 py-3 rounded-xl gradient-primary text-white font-semibold shadow-lg hover:opacity-90 transition-all">                  Done                </motion.button>              </div>            )}          </motion.div>        </div>      )}    </AnimatePresence>  );}